### YamlMime:Module
uid: cogitave.learn.api-design
metadata:
  title: Design and version an API
  description: The two standards behind every contract surface in an AI-native org - the API design standard (contract-first, resource-oriented, RFC 9457 errors, cursor pagination, idempotency) and the versioning-and-deprecation policy - so you can shape a clean v1 and evolve it without breaking consumers.
  ms.date: 2026-07-26
  author: cogitave
  ms.topic: module
title: Design and version an API
summary: Shape a clean, consistent API against a clear API design standard - resources, standard methods, typed errors, cursor pagination, idempotency - then version and deprecate it under a versioning policy so no consumer is ever broken without consent.
abstract: |
  By the end of this module, you'll be able to:
  - Design a resource-oriented, contract-first API that meets the same shape across HTTP, gRPC, GraphQL, and MCP, using the conventions the API design standard fixes.
  - Apply the estate-wide MUST invariants - the RFC 9457 error model, the `code` taxonomy, cursor pagination bounds, and idempotency on creating writes - and know when a default needs a recorded deviation.
  - Pick the right versioning scheme per surface and evolve a contract additively without breaking a live consumer.
  - Walk a contract through the deprecation lifecycle - deprecated, sunset, retired - keeping the API, docs, and changelog in lockstep from one deprecation manifest.
prerequisites: |
  - Familiarity with HTTP APIs (methods, status codes) and reading a schema.
  - Helpful but not required: the AGENTS floor and the request lifecycle.
iconUrl: /learn/achievements/api-design.svg
ratingEnabled: true
levels:
  - intermediate
roles:
  - developer
products:
  - cogitave-core
subjects:
  - software-engineering
units:
  - cogitave.learn.api-design.introduction
  - cogitave.learn.api-design.design-a-clean-api
  - cogitave.learn.api-design.version-and-deprecate
  - cogitave.learn.api-design.knowledge-check
  - cogitave.learn.api-design.summary
badge:
  uid: cogitave.learn.api-design.badge
